DATABASE_URL="postgresql://usuario:password@host:5432/database"
AUTH_SECRET="cambia_este_valor_por_un_secreto_seguro"
TELEGRAM_BOT_TOKEN="pendiente_para_integracion_futura"

# ── Nhost ─────────────────────────────────────────────────────
# app.nhost.io → tu proyecto → Settings → Secrets
NHOST_DATABASE_URL="postgresql://usuario:password@host:5432/database"
NHOST_SUBDOMAIN=xxxxxxxxxxxx
NHOST_REGION=us-east-1

# ── Auth ──────────────────────────────────────────────────────
# Genera con: openssl rand -base64 32
JWT_SECRET=vozsegura_dev_secret_2024

# ── Telegram Bot ──────────────────────────────────────────────
# Lo configura el equipo del bot
TELEGRAM_BOT_TOKEN="pendiente_para_integracion_futura"
TELEGRAM_WEBHOOK_SECRET=cadena_aleatoria_para_validar_webhook

# ── IA — Google Gemini Flash (gratis) ─────────────────────────
# aistudio.google.com → Get API Key
GEMINI_API_KEY=AIza...

# ── Cifrado de datos sensibles ────────────────────────────────
# Genera con: openssl rand -hex 32
ENCRYPTION_KEY=64_caracteres_hex_aleatorios
